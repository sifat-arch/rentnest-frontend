/* eslint-disable @typescript-eslint/no-explicit-any */
type PropertyState = {
  success: true;
  statusCode: number;
  message: string;
  data: Record<string, any>;
};

export const createProerty = async (prevState: PropertyState, fromData:FormData) => {

  



};
